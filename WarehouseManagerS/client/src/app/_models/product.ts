export interface Product {
  productId: number;
  productName: string;
  quantityInStock: number;
  categoryName: string;
  unitPrice: number;
  // currency notImplented maybe in future with API??
}

